{
  "name": "synccode",
  "version": "1.0.0",
  "private": "true",
  "description": "",
  "main": "index.js",
  "scripts": {
    "xf:install": "concurrently \"cd web && pnpm install\" \"cd server && pnpm install\"",
    "xf:dev": "concurrently \"pnpm run-web\" \"pnpm run-server\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-web": "cd web && pnpm dev",
    "run-server": "cd server && pnpm dev"
  },
  "author": "Ajay Sehwal",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
